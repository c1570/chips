`run_pc_version.sh` to run the PC-based version (c64-ascii.c) of the C64+C1541 emulator.

`run_rp2040_version.sh` to run the RP2040 version of C1541 within rp2040js (that talks to a c64.h host via FFI).
